file(GLOB_RECURSE CXX_SRCS "*.cpp")

add_library(${PROJECT_NAME}_cxx MODULE ${CXX_SRCS})

set_target_properties(${PROJECT_NAME}_cxx PROPERTIES
  CXX_STANDARD 20
  LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}
)
