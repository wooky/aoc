file(GLOB_RECURSE CXX_SRCS CONFIGURE_DEPENDS "*.cpp")

add_library(${PROJECT_NAME} SHARED ${CXX_SRCS})

set_target_properties(${PROJECT_NAME} PROPERTIES
  CXX_STANDARD 20
  LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}
)
